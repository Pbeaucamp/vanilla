package bpm.fasd.ui.measure.definition.action;

import org.eclipse.jface.action.IAction;
import org.eclipse.jface.viewers.ISelection;
import org.eclipse.ui.IWorkbenchWindow;
import org.eclipse.ui.IWorkbenchWindowActionDelegate;

public class MeasureDefinitionAction implements IWorkbenchWindowActionDelegate  {

	@Override
	public void dispose() {
		
		
	}

	@Override
	public void init(IWorkbenchWindow window) {
		
		
	}

	@Override
	public void run(IAction action) {
//		MeasureDefinitionDialog dial = new MeasureDefinitionDialog(FreemetricsPlugin.getDefault().getWorkbench().getActiveWorkbenchWindow().getShell(), FreemetricsPlugin.getDefault().getFAModel());
//		if(dial.open() == Dialog.OK);
	}

	@Override
	public void selectionChanged(IAction action, ISelection selection) {
		
		
	}

}
