<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
	xmlns:g="urn:import:com.google.gwt.user.client.ui">
	
	<ui:with field='label' type='bpm.smart.web.client.I18N.LabelsConstants' />
	<ui:with field='resources' type='bpm.smart.web.client.images.Images' />
	<ui:with field='commons' type='bpm.gwt.commons.client.images.CommonImages' />
	<ui:style type="bpm.smart.web.client.dialogs.ProjectDialog.MyStyle">
	
		.panelContent {
			height: 450px;
			width: 690px;
			z-index:20;
		}
		
		.lblOver {
		    width: 150px;
		    padding: 10px;
		    margin-top: 0;
		    text-align: left;
		    clear: both;
		}
		
		.txtblock {
		    display: block;
		    padding: 5px;
		    margin: 0 10px;
		    width: 350px;
		}
		
		.lblDate {
		    display: inline-block;
		    width: 40px;
		}
		
		.Date {
		   display: inline; 
		}
		
		.avatar {
		    border: 1px solid;
		    float: left;
		    height: 100px;
		    margin-left: 10px;
		    width: 100px;
		    text-align: center;
		    line-height: 100px;
		    border-radius:5px;
		    background-size: cover;
		}
		
		.panelIcon {
		   position: absolute;
		   margin: 10px;
		   top:40px;
		   right: 0; 
		}
		
		.upload {
		    line-height: 30px;
   			margin-left: 0px;
   			width: 50px;
   			visibility: hidden;
		}
		
		.lblIcon {
			width: 50px;
			margin : 0;
		}

		.txtArea {
		    max-width: 400px;
		    max-height: 100px;
		}
		
		.overlay {  
		    background:rgba(0, 0, 0, .75);
		    text-align:center;
		    opacity:0;    
		    width:100%;height:100%; 
		    -webkit-transition: all 0.3s ease-in-out;
		    -moz-transition: all 0.3s ease-in-out;
		    -o-transition: all 0.3s ease-in-out;
		    -ms-transition: all 0.3s ease-in-out;
		    transition: all 0.3s ease-in-out;
		}
		.avatar:hover {
		    border:1px solid #555;
		    border-radius:5px;
		}
		.avatar:hover .overlay {
		    opacity:1;
		}
		.overlayImage {  
		    height: 24px;
		    width: 24px;
		    padding:38px;
		}
		.lblIcon {  
		    position:absolute;
		    width: 100px;
		    color: #FFFFFF;
   			height: 20px;
   			top: 30px;
		}
		
		.panelGrid {  
		  	height: 250px;
		    position: absolute;
		    right: 10px;
		    top: 170px;
		    width: 270px;
		}
	</ui:style>
	<g:HTMLPanel addStyleNames="{style.panelContent}">
		
		<g:Label ui:field="lblName" addStyleNames="{style.lblOver}"/>
		<g:TextBox ui:field="txtName" addStyleNames="{style.txtblock}"/>
		<g:Label ui:field="lblDate" addStyleNames="{style.lblOver},{style.lblDate}"/>
		<g:Label ui:field="txtDate" addStyleNames="{style.txtblock},{style.Date}"/>
		<g:Label ui:field="lblAuthor" addStyleNames="{style.lblOver}"/>
		<g:TextBox ui:field="txtAuthor" addStyleNames="{style.txtblock}"/>
		<g:HTMLPanel addStyleNames="{style.panelIcon}">
			<!-- <g:Label ui:field="lblIcon" addStyleNames="{style.lblOver},{style.lblIcon}"/> -->
			<g:HTMLPanel ui:field="avatar"  addStyleNames="{style.avatar}" >
				
				<g:HTMLPanel addStyleNames="{style.overlay}">
					<g:Label text="{label.Icon}" addStyleNames="{style.lblIcon}"/>
				    <g:Image ui:field="overlay" resource="{commons.add_24}" addStyleNames="{style.overlayImage}"/>
				</g:HTMLPanel>
			</g:HTMLPanel>
			<g:FormPanel ui:field="formAvatar"/>
			<g:SimplePanel ui:field="panelUpload" addStyleNames="{style.upload}"/>
		</g:HTMLPanel>
		<g:Label ui:field="lblStatus" addStyleNames="{style.lblOver}"/>
		<g:SimplePanel ui:field="panelStatus" addStyleNames="{style.txtblock}"/>
		<g:Label ui:field="lblSubject" addStyleNames="{style.lblOver}"/>
		<g:TextArea ui:field="txtSubject" addStyleNames="{style.txtblock},{style.txtArea}"/>
		
		<g:HTMLPanel ui:field="datasetsGrid" addStyleNames="{style.panelGrid}">
			<g:Label ui:field="lblDatasets" addStyleNames="{style.lblOver}"/>
		</g:HTMLPanel>
		
	</g:HTMLPanel>
</ui:UiBinder> 